# Pomegranate
This is a method for storing your Android phone's configuration as a directory!

## To use
You must have `adb` installed (on Nix it's under the package `androidenv.androidPkgs.androidsdk`)
