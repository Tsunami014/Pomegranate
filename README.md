# Pomegranate
This is a method for storing your Android phone's configuration as a directory!

## To install
All this requires to run is the `adb-shell[usb]` python package (a `shell.nix` is provided for Nix users).
The phone must be connected via usb and have USB debugging enabled.
